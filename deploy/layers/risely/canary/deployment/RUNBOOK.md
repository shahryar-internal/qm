# CEO canary live-shadow task

This Terraform slice defines the first production-shaped CEO-canary task without activating it. It uses the existing Risely QM ECS cluster and PostgreSQL RDS instance whose database is exactly `qm`. It does not reference or permit a Command Center database, provider credential, provider route, evaluation-writer credential, public listener, or public task address.

Authorized state inspection on 2026-08-27 confirmed that no CEO-canary ECR repository, log group, service-discovery service, task definition, ECS service, IAM role, secret, subnet, route table, security group, or endpoint existed in the Terraform state. The dedicated task definition and ECS service were therefore additive resources with no legacy address to move. The authorized refresh-enabled plan at state serial 15 and lineage `5fce8eb8-4c2e-0035-9d66-d549ecbcb530` produced exactly 30 creates, 2 in-place updates, 95 no-ops, and no destroy or replacement action. Its apply added the isolated canary resources, attached the additive RDS security group, and narrowed the generic GitHub deploy policy to six non-canary services. A later authorized refresh-enabled plan against the same default workspace and unchanged lineage replaced only the inert task definition with `risely-qm-pilot-ceo-canary:2` and updated the zero-count service pointer to that revision. Post-apply state serial is 19. The registered task uses exact image digest `sha256:264263fc1698d169c55f14c86d39164db84b1517c8e10ba4ffaf28605dcafc69`; the service has zero desired, running, and pending tasks; and the database-operator slice remains absent. A fresh refresh-enabled plan at serial 19 reported zero changes. Neither apply launched a task, activated the canary, or produced provider traffic. Any later apply requires a newly generated authorized plan against the same backend, workspace, and lineage because each reviewed plan was consumed and must never be reused.

The generic GitHub deployment role cannot register the canary task family, update the canary service, push or delete canary ECR images, pass the canary roles, read or mutate canary secrets, or read canary logs. Its shared inventory reads remain read-only and cannot deploy the canary. No replacement canary deployment principal exists in this configuration. Activation requires a separately reviewed, currently unavailable principal with exact canary-only resources. The canary task role has an explicit deny-all inline policy, so an out-of-band allow cannot grant AWS API authority. Live IAM inventory was completed for the authorized inert apply and must be repeated before scaling or activation. The offline activation evaluator intentionally retains its IAM and state-lineage external-evidence slots as unmet because repository hashes cannot prove current AWS state.

`ceo_canary_runtime_image` has no variable default. The tracked deployment input now supplies the complete stack-owned ECR repository URI pinned to `sha256:264263fc1698d169c55f14c86d39164db84b1517c8e10ba4ffaf28605dcafc69`; tags and foreign repositories fail validation. The task receives only `CANARY_DATABASE_URL`, `CANARY_INGRESS_SECRET`, and `DATABASE_CA_CERT`; the last environment variable maps only to the dedicated `CANARY_DATABASE_CA_CERT` secret container. Its execution role can fetch only those three canary-prefixed secret resources, so the generic deploy role cannot mutate the canary trust anchor through the shared `DATABASE_CA_CERT` container. The dormant QM observer source requires a fourth, route-scoped secret plus distinct audience and key-id configuration, but task revision 2 and the execution role deliberately do not bind them. Provisioning those values requires a new reviewed state-backed plan while the service remains at desired count zero. The expected Terraform output binds the digest, exact account, region, cluster, task, service, family, registry, subnet, security-group, route, endpoint, secret, IAM, logging, profile, and QM database identities.

Post-apply inspection produced current Terraform provenance digest `227d39a50c7a34fd5486c5c77d6dde630f9574752180b6c339513e409afa5909`. It captured task definition `risely-qm-pilot-ceo-canary:2`, the exact digest-pinned runtime image, the zero-count service, private RDS identity and exact two-group attachment, every canary task/endpoint/database security-group rule, both zero default-route counts, the exact local and S3 prefix-list routes, endpoint policy hashes, and endpoint identities. The closed source seal now binds that observation, while every external activation requirement remains explicitly unmet. The verifier accepts only an observation and never accepts a caller-selected expected identity. Any extra group, rule, route, widened port, changed peer, or changed policy fails comparison. Provenance does not authorize scaling, database mutation, secret population, or provider execution.

The original shared database security group remains byte-for-byte unchanged. A dedicated second database security group admits port 5432 only from the canary task security group and is additively attached to RDS. The task egress rule references only that dedicated group. The canary route contract contains only the VPC-local route and the exact S3 endpoint prefix-list route. It separately derives and rejects IPv4 `0.0.0.0/0` and IPv6 `::/0` defaults rather than treating the legitimate prefix-list route as a default. The subnets never assign public addresses. Port 8080 ingress is accepted only from the existing QM service security group and still requires the signed internal HMAC protocol.

Interface endpoints for ECR, Secrets Manager, and CloudWatch Logs have explicit least-resource policies. The S3 gateway permits only ECR layer reads. Interface endpoint private DNS is VPC-wide, so endpoint ingress and policies preserve the existing generic QM services' required control-plane access. Before any apply, an authorized state-backed plan and endpoint-policy review must prove those services remain reachable. After any authorized apply and before scaling the canary, live resolution and image-pull, secret-read, and log-write QA for representative generic QM tasks is mandatory. This QA is intentionally unavailable to offline evidence and remains a hard blocker.

The container runs as `node`, has a read-only root filesystem, cannot be privileged, drops every Linux capability, enables the init process, checks database-backed `/readyz`, uses bounded non-blocking logs, and cannot use ECS Exec. Mutations and provider execution remain fixed to `0`. The ECS service remains fixed at desired count zero and has no load balancer attachment.
